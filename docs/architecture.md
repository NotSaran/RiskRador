Overview
RiskRador Architecture

RiskRador is an end-to-end Machine Learning project designed to predict the likelihood of loan default using the Home Credit Default Risk dataset. The project follows a modular data science workflow where raw data is transformed into meaningful insights through preprocessing, feature engineering, model training, evaluation, and prediction.

The architecture is organized into independent modules so that each stage of the machine learning pipeline can be developed, tested, and maintained separately.
